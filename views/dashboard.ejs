<%- include('./partials/header') -%>
<%- include('./partials/navbar') -%>


<!-- <h1 class="mt-4">Welcome <%= user.name %>!</h1> -->

<!-- <p class="lead mb-3">Welcome <%= user.name %>!</p> -->
<!-- <br> -->
<% if (user.role === "admin") { %>
    <p class="btn btn-secondary btn-block">Admin Page</p>
    <% } %>
<div class="jumbotron">
    <div class="container" style="  border-style: solid; border-color: rgb(157, 157, 157); border-radius: .5em; border-width: 3px;">
        <div class="row">
            <div class="col" style="text-align:center;">
                <div class="row" style="margin-bottom: 5px;">
                    <h1 class="display-4" style="text-align:center">Welcome <%= user.name %>!</h1>
                </div>
                <p>Start sharing old recipes with family and friends! So you don't get stuck calling your aunt for that old recipe your grandma created on a beat up index card from the 1950's. </p>
                <div class="row" style="margin-bottom: 10px">
                    <div class="col" style="text-align:center; padding: 5px;">
                        <a class="btn btn-warning btn-lg" href="/recipes" role="button">View Recipes</a>
                    </div>
                    <div class="col" style="text-align:center; padding: 5px;">
                        <a href="/recipes/newForm" class="btn btn-success btn-lg">Add Recipe</a>
                        <!-- <a href="/users/logout" class="btn btn-success btn-lg">Logout</a> -->
                    </div>
                </div>
              
            </div>
            <br>
            <div class="col" style="text-align:center">
                <img id="preview" style="height: 15em; display: inline-block; align-items: center;" src="<%=user.profileImgURL%>">
            </div>
        </div>
    </div>
   <br>
    <h2 style="margin-top: 10px; text-align: center; font-size: 1.5rem"><%= user.name %>'s Recipes</h2>
    <hr>
    <div class="container">
        <div class="flex-parent">
            <% for (var i = 0; i < recipes.length; i++) { %>
                <% if (recipes[i].author == user.name) { %> 
                    <div class="flex-item" style="text-align: center">
                        <a href="/recipes/<%= recipes[i].id %>">
                            <img class="thumbnail" src="<%= recipes[i].imageName %>">
                            <br>
                            <h8><%=recipes[i].recipeName%></h8>
                        </a>
                    </div>
                <% } %> 
            <% } %> 
      </div>
    </div>
    <hr class="my-4">
    
    <p class="lead">
        <a href="/users/logout" class="btn btn-secondary">Logout</a>
    </p>
  </div>








<%- include('./partials/footer') -%>



