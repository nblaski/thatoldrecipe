
<%- include('../partials/header') -%>
<%- include('../partials/navbar') -%>
<%- include ('../partials/messages') -%>

<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      <h1 class="text-center mb-3">
        <i class="fas fa-user"></i>  <%=user.name %>'s' Profile
      </h1>
       
            <form class="" action="/profile" method="POST" enctype="multipart/form-data">
                <input type="file" id="file" name="file">
                <p id="status">Please select a file</p>
                <img id="preview" style="width: 200px;" src="/images/default.png">
                <div>
                    <input type="hidden" id="avatar-url" name="avatar-url" value="/images/default.png">
                </div>
                <div>
                    <input type="text" name="username" placeholder="Username"><br>
                </div>
                <div>
                    <input type="text" name="full-name" placeholder="Full name"><br><br>
                </div>
                <button type="submit" class="btn btn-primary btn-block">
                Update Profile
                </button>
      </form>
       
    </div>
  </div>
</div>


<%- include('../partials/footer') -%>


<!-- <script>
    (() => {
  document.getElementById("file-input").onchange = () => {
    const files = document.getElementById('file-input').files;
    const file = files[0];
    console.log(file)
    if(file == null){
      return alert('No file selected.');
    }
    getSignedRequest(file);
  };
})();

function getSignedRequest(file){
  const xhr = new XMLHttpRequest();
  xhr.open('GET', `https://thisoldrecipe.herokuapp.com/profile/sign-s3?file-name=${file.name}&file-type=${file.type}`);
  xhr.onreadystatechange = () => {
    if(xhr.readyState === 4){
        console.log("readyState" + xhr.readyState)
      if(xhr.status === 200){
          console.log("status" + xhr.status)
          console.log("xhrrespons" + xhr.responseText);
        const response = JSON.parse(xhr.responseText);
        console.log("response" + response)
        uploadFile(file, response.signedRequest, response.url);
      }
      else{
        alert('Could not get signed URL.');
      }
    }
  };
  xhr.send();
}

function uploadFile(file, signedRequest, url){
    console.log(file)
    console.log(signedRequest)
    console.log(url)
  const xhr = new XMLHttpRequest();
  xhr.open('PUT', signedRequest);
  xhr.onreadystatechange = () => {
    if(xhr.readyState === 4){
      if(xhr.status === 200){
        document.getElementById('preview').src = url;
        document.getElementById('avatar-url').value = url;
      }
      else{
        alert('Could not upload file.');
      }
    }
  };
  xhr.send(file);
}
</script> -->






